apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tripla-apps.frontendFullname" . }}
  labels:
    {{- include "tripla-apps.frontendLabels" . | nindent 4 }}
spec:
  replicas: 2
  selector:
    matchLabels:
      {{- include "tripla-apps.frontendSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "tripla-apps.frontendLabels" . | nindent 8 }}
    spec:
      containers:
        - name: nginx
          image: nginx:1.16.0
          ports:
            - containerPort: 8080
